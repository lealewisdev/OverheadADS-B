services:
 heliwatch:
    container_name: overheadadsb
    command: python main.py
    environment:
      - LAT=${FEEDER_LAT}
      - LON=${FEEDER_LON}
      - RAD=${HELIWATCH_RAD_NAUTICAL_MILES}
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./overheadadsb:/data
    ports:
      - 8003:8003
      - 8004:8004
    restart: unless-stopped
